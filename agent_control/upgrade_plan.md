# RAG Upgrade Plan

Goal:
Upgrade RAG microservice to hybrid retrieval architecture.

Targets:

- Add object storage (MinIO)
- Add metadata DB (PostgreSQL)
- Add vector DB (Qdrant)
- Add keyword index (Elasticsearch)
- Add Redis caching
- Implement hybrid retrieval
- Implement reranking