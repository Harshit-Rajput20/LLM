version: '3.8'

services:
  api-gateway:
    build: ./services/api-gateway
    ports:
      - "8000:8000"
    environment:
      - RAG_SERVICE_URL=http://rag-service:8001
      - PROMPT_BUILDER_URL=http://prompt-builder:8002
      - LLM_CLIENT_URL=http://llm-client:8003
      - RESPONSE_PROCESSOR_URL=http://response-processor:8004
    depends_on:
      - rag-service
      - prompt-builder
      - llm-client
      - response-processor

  rag-service:
    build: ./services/rag-service
    ports:
      - "8001:8001"

  prompt-builder:
    build: ./services/prompt-builder  
    ports:
      - "8002:8002"

  llm-client:
    build: ./services/llm-client
    ports:
      - "8003:8003"

  response-processor:
    build: ./services/response-processor
    ports:
      - "8004:8004"
